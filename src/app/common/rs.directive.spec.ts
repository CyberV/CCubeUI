import { RsDirective } from './rs.directive';

describe('RsDirective', () => {
  it('should create an instance', () => {
    const directive = new RsDirective();
    expect(directive).toBeTruthy();
  });
});
